# 项目全流程管理平台

## 项目概览
项目管理系统，包含6大模块：统计概述、项目管理、费用项管理、分包商管理、合同信息、结算管理、收入管理。采用原生HTML/CSS/JS实现，数据存储使用localStorage。

## 技术栈
- **前端**: HTML5 + CSS3 + Vanilla JavaScript
- **存储**: localStorage
- **样式**: 自定义CSS（Flex/Grid布局）

## 项目结构
```
/workspace/projects/
├── index.html          # 主页面（包含所有模块）
├── styles/
│   └── main.css         # 样式文件
└── .coze               # 配置文件
```

## 6大模块功能

### 1. 统计概述模块
项目核心指标统计看板，包含以下数据：

| 指标 | 数据来源 |
|------|----------|
| 预计整体收益率 | 效益分析：[(预计总收入不含税 - 预计总成本不含税 + 管理费) / 预计总收入含税] × 100% |
| 预计利润率 | 效益分析：[(预计总收入不含税 - 预计总成本不含税) / 预计总收入含税] × 100% |
| 结算单数量 | 合同结算单数量 + 费用项结算单数量 |
| 预估最终结算含税金额 | 效益分析：预计总收入（含税） |
| 当前已发生成本 | 结算管理：所有结算单的(已结算金额 + 未入账成本)之和 |
| 实付金额（累计） | 结算管理：所有已支付结算单的实付金额 + 合同预付款之和 |
| 项目总收入 | 收入管理：累计合同约定实付金额之和 |

### 2. 效益分析模块
**手动录入字段**：
- 预计总收入（含税/不含税）
- 自施部分收入（不含税）
- 预计总成本（含税/不含税）
- 自施部分成本（不含税）
- 管理费

**自动计算字段**（所有金额保留2位小数）：
- 预计整体收益 = 预计总收入（不含税）- 预计总成本（不含税）+ 管理费
- 预计利润 = 预计总收入（不含税）- 预计总成本（不含税）
- 预计整体收益率 = (预计整体收益 / 预计总收入（含税）) × 100%
- 预计利润率 = (预计利润 / 预计总收入（含税）) × 100%

### 3. 费用项模块
- 支持增、删、改、查
- 字段：费用名称、创建时间
- 搜索过滤功能

### 4. 分包商模块
- 支持增、删、改、查
- 字段：分包商名称、合作时间
- 搜索过滤功能

### 5. 合同信息模块
**录入字段**：
- 费用名称（下拉选择）
- 分包商名称（下拉选择）
- 合同编号、签订时间
- 保留金比例、执行中/完工待结算/结算完成付款比例
- 合同税率、原合同金额、补充协议金额、预付款金额
- 合同状态（未开始/进行中/已完成）
- 说明

**功能**：
- 增、删、改、查、查看详情
- 状态切换
- 按状态筛选

### 6. 结算管理模块

#### 5.1 合同结算
**选择合同后自动带出信息**：
- 费用名称、分包商名称、合同编号、签订时间
- 保留金比例、合同税率、原合同金额、补充协议金额、预付款

**自动计算**：
- 合计金额 = 原合同金额 + 补充协议金额
- 预估最终结算含税金额 = 合计金额
- 预估最终结算不含税金额 = 含税金额 / (1 + 税率/100)
- 税金 = 不含税金额 × 税率/100

**用户录入**：
- 自开累已结算金额
- 未入账成本（本次结算金额）
- 结算说明

**自动计算（结算维度）**：
- 当前已发生成本 = 已结算金额 + 未入账成本
- 当前已发生成本不含税 = 已发生成本 / (1 + 税率/100)
- 结算完成比例 = (已发生成本 / 合计金额) × 100%
- 除税金额 = 不含税金额

**支付功能**：
- 输入实付金额
- 按合同应付金额（包括预付款） = 预付款 + 当前已发生成本 × 执行中付款比例
- 显示按合同应付金额、实付金额、欠付金额

#### 5.2 费用项结算
- 选择费用名称
- 输入结算金额、说明
- 支持增、删、改、查、查看详情
- 支付状态管理

### 7. 收入管理模块

**录入字段**：
- 产值周期
- 文件编号
- 实际完成产值
- 甲方确认产值
- 合同约定应付金额
- 扣款
- 合同约定实付金额（自动计算：合同约定应付金额 - 扣款）
- 备注

**功能**：
- 增、删、改、查
- 搜索过滤
- 顶部统计累计合同约定实付金额

## 数据存储
使用localStorage存储，包含以下key：
- `project_benefits` - 项目效益数据
- `project_expenses` - 费用项列表
- `project_subcontractors` - 分包商列表
- `project_contracts` - 合同列表
- `project_contract_settlements` - 合同结算单列表
- `project_expense_settlements` - 费用项结算单列表
- `project_income` - 收入列表

## 验证命令
```bash
# 检查服务状态
curl -I http://localhost:5000

# 检查日志
tail -n 50 /app/work/logs/bypass/app.log
```

## 开发说明
- 服务运行端口：5000
- 所有计算公式在前端自动完成，修改原始值后立即重新计算
- 使用Tab切换模块，卡片式布局
- 响应式设计，支持移动端
